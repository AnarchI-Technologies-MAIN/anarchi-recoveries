# Disposable loopback identity proof only. Resolve production secrets through the governed broker/Vault path.
RECOVERIES_ZITADEL_TEST_DB_PASSWORD=replace-with-url-safe-local-test-value
RECOVERIES_ZITADEL_TEST_MASTERKEY=replace-with-exactly-32-characters
